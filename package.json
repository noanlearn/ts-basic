{
  "name": "ts-basic",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf build/ && tsc",
    "ts": "rm -rf build/ && tsc -w",
    "dev": "nodemon ./build/function.js"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "typescript": "^5.3.2"
  }
}
